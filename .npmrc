# Suppress the "N packages are looking for funding" nag on every install.
# Security audit stays ON (audit=true is npm's default) — we want real
# vulnerabilities to surface, just not the funding spam. Run `npm audit`
# explicitly whenever you want the full report.
fund=false
